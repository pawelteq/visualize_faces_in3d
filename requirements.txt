# --- Rdzen pipeline'u ---
numpy
Pillow
scikit-learn
pyvista
opencv-python        # CLAHE - normalizacja oswietlenia

# --- Backend ArcFace (domyslny) ---
# Mocniej wazy twarz niz swiatlo. Przy pierwszym uruchomieniu pobiera model (buffalo_l).
insightface
onnxruntime          # lub onnxruntime-gpu, jesli masz CUDA (szybsze przy wielu zdjeciach)

# --- Backend dlib (opcjonalny, lzejszy: --backend dlib) ---
# Wymaga dlib, ktory na Windows potrzebuje CMake + Visual Studio Build Tools (C++).
# face_recognition
