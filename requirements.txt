# --- Rdzen pipeline'u ---
numpy
Pillow
scikit-learn
pyvista
opencv-python        # CLAHE - normalizacja oswietlenia
umap-learn           # --reduce UMAP (najlepsze rozdzielanie grup w wizualizacji)
metric-learn         # --metric lmnn (uczenie metryki z etykiet; LDA/NCA sa w scikit-learn)

# --- Backend ArcFace (domyslny) ---
# Mocniej wazy twarz niz swiatlo. Przy pierwszym uruchomieniu pobiera model (buffalo_l).
insightface
# GPU (zalecane przy duzej liczbie zdjec) - wymaga sterownikow CUDA + cuDNN:
onnxruntime-gpu
# Bez GPU uzyj zamiast powyzszego:
# onnxruntime

# --- Filtr CLIP (opcjonalny: --clip-filter) ---
# Odrzuca psy/zwierzeta/grafiki, ktore detektor blednie lapie jako twarze.
# Najlepiej wersja torch z CUDA (https://pytorch.org).
# open_clip_torch
# torch

# --- Backend dlib (opcjonalny, lzejszy: --backend dlib) ---
# Wymaga dlib, ktory na Windows potrzebuje CMake + Visual Studio Build Tools (C++).
# face_recognition
